# delegation-heavy compaction prompt

Compact this delegation-heavy Codex session.

Preserve orchestrator objective, delegated task boundaries, subagent artifact paths, owned files/write scopes, return summaries, review disposition, conflicts, verification performed, and integration status.

Do not preserve full subagent outputs if durable artifacts exist. Preserve paths and disposition instead.

Structure the output as: orchestrator objective; subagent table; artifact paths; accepted/revised/parked/rejected outputs; conflicts and risks; verification; next integration actions.
