# setup-config compaction prompt

Compact this Codex setup/configuration session.

Preserve installed/candidate files, config keys observed, config keys proposed, profiles discussed, sandbox/approval assumptions, hooks/rules status, skills/agents/plugin install mode, project trust state, unresolved platform-mechanics questions, and next setup actions.

Do not overclaim effective runtime behavior unless it was probed. Mark values as observed, inferred, candidate, unknown, or managed/overridden.

Structure the output as: setup objective; observed current setup; proposed candidates; active uncertainties; accepted/rejected choices; files/artifacts; next setup actions.
